# NOTES — Прошивка МК клавиатуры Pinebook Pro через внешний ISP-программатор

> Сценарий: вы собираете `.hex` на отдельном ПК, затем подключаетесь
> **внешним ISP/ICP-программатором напрямую к микроконтроллеру** и прошиваете
> его специализированной утилитой Sinowealth (не через USB-бутлоадер PBP).

---

## 0. Краткий итог (TL;DR)

| Чип в вашей клавиатуре | Тип памяти | Утилита SinoWealth | Программатор |
|---|---|---|---|
| **SH68F83** | 16K Flash | **ProWriter** | ICP-программатор (6 проводов: VDD, GND, TCK, TDI, TMS, TDO) |
| **SH61F83** | 14K MTP (**всего 8 циклов!**) | **HidUpdate4.exe** | USB ISP boot-цепь (D+, D−, VBUS, GND + 2×300K) |

> ⚠️ **Сначала определите модель чипа**, прежде чем что-либо делать.
> В Pinebook Pro обычно стоит **SH68F83** (Flash), но встречаются ревизии с
> **SH61F83** (MTP, 8 циклов перезаписи — чужеродная прошивка может
> **безвозвратно** его убить).

---

## 1. Что за чип и чем его шить

### 1.1. SH68F83 (основной вариант в PBP)

- Ядро: 8051-совместимое, 16 КБ Flash-памяти (`0x0000`–`0x3FFF`)
- Сектор: **1024 байта** (всего 16 секторов)
- **Два режима программирования** (datasheet, раздел 11):

| Режим | Что это | Чем делается |
|---|---|---|
| **ICP** (In-Circuit Programming) | Внешний программатор через 6-проводной интерфейс | Sinowealth Flash Programmer + **ProWriter** |
| **SSP** (Self Sector Programming) | Сам чип шьёт себя из кода (через USB-бутлоадер) | `updater` из этого репо |

- **Mass Erase** доступен **только в ICP** (внешним программатором).
- Sector Erase и Write/Read — в обоих режимах.
- Это именно тот сценарий, который вам нужен: отдельный программатор → ICP.

### 1.2. SH61F83 (вариант с MTP — осторожно!)

- 14 КБ **MTP** (Multiple Time Programmable) памяти
- **Всего ~8 циклов перезаписи** на весь срок жизни чипа
- Шьётся утилитой **HidUpdate4.exe** (от Sinowealth)
- Требует специальную USB ISP boot-цепь (аппаратный способ) либо поддержка
  бутлоадера в самой прошивке (программный способ)
- Адреса `0x37F0`–`0x37FF` — зарезервированы, не использовать

> 🔴 **Если у вас SH61F83 — каждый цикл на счету. Не прошивайте «для теста».**

---

## 2. ISP/ICP-пины SH68F83 (6-проводной интерфейс)

Чип в корпусе **QFN-48 (6×6 мм)**. ICP-пины мультиплексированы с портами P00–P03
(datasheet, раздел «Pin and Pad Description»):

| ICP-сигнал | Вывод чипа (pin) | Порт | Назначение |
|---|---|---|---|
| **TCK** | 14 | P03/TCK | Тактовый сигнал программатора |
| **TDI** | 13 | P02/TDI | Данные в чип |
| **TDO** | 11 | P00/TDO | Данные из чипа |
| **TMS** | 12 | P01/TMS | Mode select |
| **VDD** | 41 (42, 43) | VDD | +5 В питание |
| **GND** | 44, 45, 46 | GND | Земля |

Дополнительно для программатора:
- **RSTB** (pin 10) — сброс (программатор удерживает для входа в ICP)

### 2.1. Как МК входит в ICP-режим

Из datasheet (раздел 11.2):

> *The SH68F83 will enter ICP mode once specified waveform of TCK, TDI, TMS and
> TDO pins is detected within a limited period after system POR.*

То есть программатор после включения питания чипа выдаёт спецпоследовательность
на TCK/TDI/TMS/TDO, и чип входит в режим ICP. Детали таймингов — в документации
к конкретному программатору Sinowealth.

### 2.2. Проблема с PBP: ICP-пины не выведены на тестовые точки

В PBP на плате выведены тестовые точки `T27/T33/T41/T38` (VDD, D+, D−, GND) —
это для USB (SSP-путь). **ICP-пины (P00–P03) на тестовые точки НЕ выведены** —
они уходят в матрицу клавиатуры через FPC-разъём.

**Варианты доступа к ICP-пинам на PBP:**

1. **Пайка напрямую к выводам QFN-48** — выводы 11/12/13/14 (P00–P03) + 10 (RSTB)
   + 41 (VDD) + 44/45/46 (GND). QFN-пады очень мелкие, нужен опыт пайки
   и тонкий провод (Ø 0.1 мм).
2. **Через FPC-разъём клавиатуры** — P00–P03 задействованы в матрице клавиш
   (см. таблицу в `firmware/src/main.c` / `fw_ansi.a51`).
   Можно временно отключить FPC и подать сигналы программатора на нужные
   контакты разъёма. **Сначала сверьте распиновку** по схеме PBP v2.1
   и дизассемблированной таблице портов.
3. **Через дорожки на плате** — найти ближайшие к чипу доступные точки
   на дорожках P00–P03 под лупой.

> ⚠️ Перед ICP-прошивкой **обязательно** отключить FPC клавиатуры
> и/или изолировать матрицу — иначе клавиатурная матрица будет мешать
   программатору (datasheet, рис. 11-1: «jumper must be open to separate
   the programming pins from the application circuit»).

---

## 3. Инструменты SinoWealth для ICP/ISP

### 3.1. SH68F83 → ProWriter

- **ПО:** `ProWriter` (утилита SinoWealth для SH68F83, Windows)
- **Программатор:** оригинальный ICP-программатор SinoWealth для SH68F83,
  подключается к ПК по USB и к чипу 6-проводным шлейфом
- **Функции:** Mass Erase / Sector Erase / Write / Read / Verify
- Порядок работы (типовой):
  1. Подключить программатор к ПК (USB)
  2. Запустить `ProWriter`
  3. Выбрать чип: `SH68F83`
  4. Загрузить файл: `.hex` (Intel HEX) из папки `firmware/`
  5. Подключить 6-проводной ICP-шлейф к чипу (TCK/TDI/TMS/TDO/VDD/GND)
  6. **Mass Erase** (полная очистка — только в ICP)
  7. **Write** (запись)
  8. **Verify** (сравнение)

### 3.2. SH61F83 → HidUpdate4.exe

- **ПО:** `HidUpdate4.exe` (Windows)
- **Программатор:** **USB ISP boot-цепь** (не отдельный программатор, а схема
  из 2 резисторов 300K и светодиода — см. datasheet SH61F83, рис. 11-1)
- **Важно:** всего ~8 циклов перезаписи!
- Порядок работы (аппаратный способ):
  1. Собрать USB ISP boot-цепь: D− через 300K к VBUS, D+ через 300K к GND,
     светодиод + 330R на D+
  2. Подключить USB-кабель устройства к boot-цепи (D+/D− задействованы)
     и подать +5 В
  3. После мигания светодиода — отключить от boot-цепи
  4. Подключить USB-кабель напрямую к ПК
  5. Запустить `HidUpdate4.exe`
  6. `BURN NUM` — посмотреть оставшееся число циклов
  7. `LOAD FILE` — выбрать `.hex`
  8. `WRITE IC` — Erase/Program/Verify
  9. `EXIT`, переподключить USB

### 3.3. Если программатор не от SinoWealth (сторонний/универсальный)

1. Проверить в документации программатора, поддерживает ли он **SH68F83**
   или **SH61F83** (ядро 8051 + проприетарный ICP-протокол SinoWealth).
2. Найти ПО производителя программатора с поддержкой этих чипов.
3. Обратиться к сообществу Pinebook Pro / Pine64 — бывают наработки
   по нестандартным программаторам.
4. **Альтернатива без ISP:** использовать USB-бутлоадер (SSP) через `updater`
   из этого репозитория — см. корневой `README.md`.

---

## 4. Полный сценарий: сборка на отдельном ПК → ICP-прошивка

### Шаг 1. Сборка прошивки на отдельном ПК

```bash
# Зависимости: sdcc (Small Device C Compiler) и srecord
sudo apt-get install sdcc srecord

# Клонировать репозиторий
git clone https://github.com/dragan-simic/pinebook-pro-keyboard-updater.git
cd pinebook-pro-keyboard-updater

# Собрать готовую раскладку (результат: firmware/<имя>.hex)
make -C firmware/src default_ansi     # ANSI
# либо: make -C firmware/src default_iso
# либо: make -C firmware/src colemak_ansi / dvorak_ansi / jack_ansi
```

Готовые `.hex` (без сборки) уже лежат в `firmware/`:

| Файл | Назначение |
|---|---|
| `fw_ansi.hex` | Оригинальный стоковый образ (ANSI) — **база** |
| `fw_iso.hex` | Оригинальный стоковый образ (ISO) |
| `default_ansi.hex` | Кастомная клавиатура с фиксами (ANSI) |
| `default_iso.hex` | Кастомная клавиатура с фиксами (ISO) |
| `colemak_ansi.hex` / `dvorak_ansi.hex` / `jack_ansi.hex` | Альтернативные раскладки |

> Для ICP (Mass Erase + полная перезапись) берите **полный образ**:
> `fw_ansi.hex` / `fw_iso.hex` или пересоберите кастомный поверх него.
> Кастомные `default_*.hex` — это **оверлеи** (генерируются наложением на
> `fw_ansi.hex` через сборку); для ICP нужен именно полный `.hex`.

### Шаг 2. Подготовка доступа к ICP-пинам

1. Выключить PBP, отключить батарею.
2. Отключить FPC-шлейф клавиатуры от основной платы.
3. Подключиться к ICP-пинам чипа SH68F83 (см. раздел 2):
   - TCK → P03 (вывод 14)
   - TDI → P02 (вывод 13)
   - TMS → P01 (вывод 12)
   - TDO → P00 (вывод 11)
   - VDD → +5 В (вывод 41, тестовая точка T27)
   - GND → земля (вывод 44/45/46, тестовая точка T38)
   - RSTB → вывод 10 (управление сбросом)

### Шаг 3. Прошивка через ProWriter (SH68F83)

1. Подключить ICP-программатор SinoWealth к ПК по USB.
2. Запустить `ProWriter`, выбрать чип **SH68F83**.
3. `LOAD FILE` → выбрать `firmware/fw_ansi.hex` (или ваш собранный полный `.hex`).
4. Подать питание на чип (через программатор или отдельный +5 В).
5. **Mass Erase** — полная очистка flash.
6. **Write** — запись образа.
7. **Verify** — контрольное чтение и сравнение.
8. Снять питание, отключить программатор, убрать временные провода,
   вернуть FPC-шлейф клавиатуры.

### Шаг 4. Проверка

1. Собрать PBP, подключить питание.
2. Клавиатура и тачпад должны определиться по USB как
   `VID:258a PID:001e` / `PID:001f`.
3. Если тачпад не работает после прошивки только клавиатурного образа —
   выполнить `sudo ./updater step-2 ansi` (или `iso`) для обновления
   прошивки тачпада.

---

## 5. Предупреждения и риски

| Риск | Описание |
|---|---|
| 🔴 **SH61F83: лимит 8 циклов** | Каждая перезапись необратимо расходует ресурс. Перед прошивкой убедитесь, что образ корректен. |
| 🟠 **Ошибки ICP-коммутации** | Неправильное подключение TCK/TDI/TMS/TDO или отсутствие питания может «подвесить» чип. Сверяйтесь с распиновкой. |
| 🟠 **Конфликт с матрицей клавиатуры** | P00–P03 задействованы в матрице. Без отключения FPC программатор может не увидеть чип. |
| 🟡 **Отсутствие тестовых точек ICP** | На плате PBP нет готовых площадок под ICP — нужна пайка к выводам QFN-48. |
| 🟡 **Несовместимый программатор** | Не каждый универсальный программатор поддерживает проприетарный ICP-протокол SinoWealth. |

---

## 6. Ссылки и материалы

- Datasheet SH68F83: [`firmware/docs/sinowealth-sh68f83-datasheet-v2.pdf`](firmware/docs/sinowealth-sh68f83-datasheet-v2.pdf)
  — раздел **11 «Flash Program Memory»** (ICP/SSP), раздел «Pin and Pad Description» (пины TCK/TDI/TMS/TDO).
- Datasheet SH61F83: [`firmware/docs/sinowealth-sh61f83-datasheet-v2.pdf`](firmware/docs/sinowealth-sh61f83-datasheet-v2.pdf)
  — раздел **11 «MTP Program Memory»** (HidUpdate4.exe, USB ISP boot-цепь, 8 циклов).
- Схема PBP v2.1: http://files.pine64.org/doc/PinebookPro/pinebookpro_v2.1_mainboard_schematic.pdf
- Распиновка тестовых точек PBP: [`firmware/README.md`](firmware/README.md) (раздел «Hardware»)
- Исходники прошивки: [`firmware/src/`](firmware/src/)
- Дизассемблер стока: [`firmware/dis51/`](firmware/dis51/)

---

## 7. Быстрая шпаргалка

```bash
# 1) Собрать прошивку на отдельном ПК:
make -C firmware/src default_ansi   # → firmware/default_ansi.hex

# 2) Для ICP-прошивки нужен ПОЛНЫЙ образ, не оверлей.
#    Полные образы: fw_ansi.hex, fw_iso.hex
#    (кастомный default_*.hex = оверлей поверх fw_ansi.hex,
#     собранный srec_cat в полный файл — тоже подходит)

# 3) ICP-пины SH68F83 (QFN-48):
#    TCK=pin14(P03), TDI=pin13(P02), TMS=pin12(P01), TDO=pin11(P00),
#    VDD=pin41, GND=pin44/45/46, RSTB=pin10

# 4) SH68F83  → ProWriter  (ICP-программатор SinoWealth)
#    SH61F83  → HidUpdate4.exe  (USB ISP boot-цепь, всего 8 циклов!)